## Files and Setup

- **Implementation file:** `pass/PrefetchIMA.cc`
- **Build project:**  
```bash
./build.sh
```

- **Run testcases:**
```bash
./run_tests.sh
```

# For mac based machine, replace prefetchIMAPass.so with prefetchIMAPass.dylib in run_tests.sh 
# For linux based machine, replace prefetchIMAPass.dylib with prefetchIMAPass.so in run_tests.sh

